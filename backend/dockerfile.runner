FROM python:3.11-slim

RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*
RUN pip install pytest psutil

WORKDIR /runner
COPY runner_entrypoint.sh /runner_entrypoint.sh
RUN chmod +x /runner_entrypoint.sh

ENTRYPOINT ["/runner_entrypoint.sh"]